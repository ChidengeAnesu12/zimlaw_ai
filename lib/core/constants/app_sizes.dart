/// Spacing scale used across the app. Started minimal — we add more
/// tokens as repeated values emerge across screens, rather than
/// guessing every spacing value up front.
class AppSizes {
  AppSizes._();

  static const double sm = 8;
  static const double md = 16;
  static const double lg = 24;
  static const double xl = 32;

  static const double screenPaddingH = 24;
  static const double buttonHeight = 56;
}